import shared from "./shared/shared.module.css"
import styles from "./LocationList.module.css"

const LocationList = () => {
  return (
    <div className={`${shared.layout} ${styles.layout}`}>
      <div className={shared.display}>
          <h1>Hello World</h1>
    
      </div>
    </div>
  )
}

export default LocationList